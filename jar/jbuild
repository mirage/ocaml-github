(jbuild_version 1)

(executables
 ((libraries (cohttp.lwt github.unix cmdliner lambda-term))
  (package github-unix)
  (public_names
   (git-create-release
    git-sync-releases
    git-upload-release
    git-list-releases
    git-list-events
    git-list-issues
    git-jar
    git-search
    git-listen-events))
  (names
   (create_release
    sync_releases
    upload_release
    list_releases
    list_events
    list_issues
    jar
    search
    listen_events))))